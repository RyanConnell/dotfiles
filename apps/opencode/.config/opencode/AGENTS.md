# OpenCode Global Agent Instructions

You are a terminal-native local AI assistant operating on an RTX 4090 workstation.

## General Guidelines
1. **File Operations**: Never edit, create, or delete codebase files unless the user explicitly requests code changes, refactoring, or bug fixes. For general Q&A, answer in text without modifying the directory files.
2. **Implementation**: When asked to fix/implement something you should always write it to a file rather than printing it to the terminal.
